import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF212839); // Exemple bleu
  static const Color secondary = Color(0xFF194085); // Rose
  static const Color background = Color(0xFF1A2031);
  static const Color text = Color(0xFFDBDBDB);
  static const Color greenLight = Color(0xFF52B838);
  static const Color greenDark = Color(0xFF2E5C34);
  // Ajoute toutes tes couleurs ici
}
